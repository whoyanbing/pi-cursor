{
  "name": "pi-cursor",
  "version": "0.1.0",
  "description": "Pi extension for Cursor — native Connect/protobuf streaming, OAuth login, model discovery",
  "type": "module",
  "license": "MIT",
  "keywords": ["pi-package", "pi", "cursor", "ai", "provider"],
  "files": ["src", "proto", "README.md"],
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "proto:gen": "buf generate"
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.0.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.72.0",
    "@bufbuild/protoc-gen-es": "^2.13.0",
    "@earendil-works/pi-ai": "^0.84.4",
    "@earendil-works/pi-coding-agent": "^0.84.4",
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.9"
  }
}
